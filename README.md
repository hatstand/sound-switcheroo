# Sound Switcheroo

Allows quick switching between output audio devices on Windows, including only the devices you actually care about.

Left click on the system tray icon to switch to the next device, or use the hotkey (default `Ctrl-alt-shift X`).

Right click on the system tray icon and select `Settings...` to configure which devices are included in the rotation
and to change the hotkey.